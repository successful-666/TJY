/**
  ******************************************************************************
  * @file    app_cmd.h
  * @brief   调试串口命令行（USART1）：手动控制电机、查看状态
  ******************************************************************************
  * @note    接收用 USART1 的 RXNE 中断，但**不需要在 CubeMX 里配置** ——
  *          本模块自行使能 NVIC 和接收中断，并自己实现 USART1_IRQHandler。
  *
  *          中断里只做"存一个字节"，命令解析与电机操作都在任务里完成
  *          （因为 485 收发是阻塞事务，绝不能放在中断上下文里）。
  ******************************************************************************
  */

#ifndef __APP_CMD_H__
#define __APP_CMD_H__

#ifdef __cplusplus
extern "C" {
#endif

/* Includes ------------------------------------------------------------------*/
#include <stdbool.h>

/* Exported functions prototypes ---------------------------------------------*/

/** @brief 使能 USART1 接收中断并创建命令解析任务 */
bool AppCmd_Init(void);

#ifdef __cplusplus
}
#endif

#endif /* __APP_CMD_H__ */
